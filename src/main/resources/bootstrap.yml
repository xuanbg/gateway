spring:
  profiles:
    active: dev
---
spring:
  profiles: dev
  application:
    name: common-gateway
  cloud:
    config:
      uri: http://localhost:6101
      label: master
      profile: dev
---
spring:
  profiles: test
  application:
    name: common-gateway
  cloud:
    config:
      uri: http://localhost:6101
      label: master
      profile: test
---
spring:
  profiles: pro
  application:
    name: common-gateway
  cloud:
    config:
      uri: http://localhost:6101
      label: master
      profile: pro